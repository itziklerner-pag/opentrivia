# Pusher Configuration (Required for the app to work)
# Get these values from your Pusher dashboard: https://dashboard.pusher.com/
PUSHER_APP_ID=your_app_id_here
PUSHER_KEY=your_key_here  
PUSHER_SECRET=your_secret_here
PUSHER_CLUSTER=mt1

# Public Pusher configuration (these match the ones in config.mjs)
NEXT_PUBLIC_PUSHER_KEY=your_key_here
NEXT_PUBLIC_PUSHER_CLUSTER=mt1

# Development URLs (usually don't need to change)
NEXT_PUBLIC_APP_URL=http://localhost:3000